
  # RARDI Landing Page

  This is a code bundle for RARDI Landing Page. The original project is available at https://www.figma.com/design/zSpCCSDJl1QipB02adZW9a/RARDI-Landing-Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  